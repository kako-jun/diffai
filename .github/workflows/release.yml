name: Release - diffai

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  release:
    uses: kako-jun/.github/.github/workflows/rust-cli-kiln-release.yml@main
    with:
      project-name: diffai
    secrets: inherit
    
